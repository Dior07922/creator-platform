# 苒境创作形态原则

> 本文件是苒境工程最高开发方向。所有代码修改先对照本文件判断是否符合创作形态，再动手。

## 核心理念

苒境不是传统软件逻辑。

不是：点击按钮 → 执行动作 → 得到结果。

而是：用户创造元素 → 元素产生关系 → 关系形成变化 → 最终形成完整创作。

按钮不是工具。按钮是**变化的开关**。

每个功能都必须思考：

- 有没有让静态变动态？
- 有没有让元素产生新的关系？
- 有没有让东西活起来？

---

## 三大核心能力

### 1. 排列

排列不是简单调整位置，不是把几个元素排整齐。

而是让不同元素（文字、图片、图形、页面）产生新的秩序。

用户应该感觉："原来这些东西可以这样组合。"

排列改变的不只是空间位置，而是：

- 视觉关系
- 内容节奏
- 表达方式

### 2. 组合

组合不是简单 Group，不是几个元素绑在一起移动。

而是形成一个新的创作单位。

原来的：文字 = 文字，图片 = 图片，图形 = 图形。

组合之后，形成一个新的对象。这个对象可以：

- 继续移动
- 继续变化
- 继续连接
- 继续参与创作

### 3. 连接

连接不是画一条线，线只是表现。真正重要的是：连接代表关系。

例如：

- 图片 → 故事
- 文字 → 页面
- 作品 → 作品

元素之间从孤立状态变成有联系的整体。

连接应该能够影响：

- 后续变化
- 播放顺序
- 创作流程
- 动态表现

---

# 当前代码判断标准

检查 Editor 实现时，不看按钮有没有。重点看：

## 排列

当前是否只是移动位置？还是排列结果能够影响作品结构、节奏、展示方式？

## 组合

当前是否只是保存一个 Group 数据？还是形成真正的新创作单位？

Unit 是否可以：整体移动、继续变化、继续连接其他 Unit？

## 连接

当前是否只是 source → target → 一条线？还是连接之后产生：

- 关系语义
- 行为
- 后续流程影响

---

# 最终目标

苒境不是工具集合，而是让创作逐渐形成生命的空间。

用户不是在操作按钮，用户是在创造一个会变化的东西。

## 开发硬性规则

1. 不删除已有逻辑
2. 不重构已有架构
3. 只允许增量修改
4. 修改前先分析影响范围
5. 画布交互必须用 pointer 事件 + touchAction，禁止只写 onMouseDown
6. 禁用 alert / confirm / prompt
7. 现有全局变量约定：window.__ranjingCommands、window.__ranjingPlayTimer
8. 新增 SheetAction 指令必须同步改 document.ts 类型
