# 苒境当前最新工程｜码道唯一执行指令

## 一、源码边界

本压缩包是当前唯一真实源码。直接在现有工程中继续，不重建项目，不用旧项目覆盖。

- 当前框架：Next.js 16 App Router。
- `app/page.tsx` 挂载 `src/App.tsx`。
- `app/api/**/route.ts` 是当前真实服务端 API。
- `vite.config.ts` 是历史遗留配置，不是当前启动入口。
- 技能商城是唯一 APP 外壳，一级页面固定为：首页、资源、创作、设计、我的。
- 禁止引入旧小说项目首页、导航、品牌或账号系统。
- 禁止修改现有小说作品、章节、人物、大纲和正文保存结构。

## 二、已封版视觉

不得重新设计欢迎页、登录页和完善资料页。

欢迎页固定：

- 苒境
- 山外，还有山。
- 进入苒境 →
- 白色浅灰水墨背景，保留远山、雾、水面、飞鸟和小舟。
- 文字均为前端真实文字，不嵌入背景图。

登录页固定：

- 小号“苒境”品牌字，约 21px，纯墨色。
- 手机号、验证码、获取验证码、未注册自动创建账号提示、进入苒境、用户协议、隐私政策。
- 已删除“创作、发现，也让好的能力被看见”。不得添加替代文案。
- 已删除方形“苒印”。不得恢复旁挂方印、红点或图标。

完善资料页固定：头像可跳过、昵称、开始使用、稍后再说。

## 三、已完成账号代码

现有代码已经包含：

- `POST /api/auth/sms/send`
- `POST /api/auth/sms/verify`
- `GET/PATCH /api/auth/me`
- `POST /api/auth/logout`
- Neon 数据库表：`users`、`sms_verifications`、`user_sessions`
- 6位服务端随机验证码，5分钟失效。
- 同手机号60秒、小时、每日及IP限流。
- 验证错误次数限制。
- 验证码摘要保存、一次性消费。
- HttpOnly、SameSite 会话 Cookie。
- 首次欢迎页状态、新老用户分流、完善资料与保持登录逻辑。

禁止增加固定验证码、模拟短信、假发送成功或假登录状态。

## 四、密钥处理

压缩包不包含 `.env.local` 和任何真实密钥。

项目所有者本人在本地或部署平台填写：

```text
AUTH_SECRET
ALIYUN_SMS_ACCESS_KEY_ID
ALIYUN_SMS_ACCESS_KEY_SECRET
ALIYUN_SMS_SIGN_NAME
ALIYUN_SMS_TEMPLATE_CODE=SMS_512275103
```

同时保留项目原有 `DATABASE_URL` 等环境变量。

不得要求项目所有者把 Secret 发到聊天；不得把密钥写入 React、源码、日志、截图或 GitHub。阿里云 RAM 账号只使用 `RanjingSmsSendOnly`，不得扩大为 FullAccess。

## 五、码道执行顺序

1. 解压并完整读取当前工程，不重建。
2. 运行 `npm install` 或按现有锁文件安装依赖。
3. 运行 `npm run build`。
4. 启动本地 Next.js。
5. 未配置密钥时验证：
   - `GET /api/auth/me` 返回 401 未登录，不得404。
   - `POST /api/auth/sms/send` 返回503登录服务尚未配置，不得404。
6. 停止并提示项目所有者本人填写环境变量。
7. 环境变量填写并重启后，只做一次真实短信端到端联调。
8. 测试新用户、老用户、刷新/重开、错误验证码、过期验证码、验证码重复使用和发送限流。
9. 阿里云失败必须显示真实失败，不进入倒计时。
10. 完成后停止，不开始改其他页面。

## 六、验收报告格式

只报告：

1. 实际运行框架和后端方式。
2. 修改/新增文件。
3. 数据库表与字段。
4. API实际状态码。
5. 使用的环境变量名称，不展示值。
6. 阿里云短信是否真实收到。
7. 新用户是否只创建一条记录。
8. 老用户是否没有重复创建。
9. 刷新和重开是否保持登录。
10. 错误、过期、重复验证码是否被拒绝。
11. 限流是否生效。
12. `npm run build` 是否通过。
13. 浏览器、服务器是否仍有404、500或控制台错误。
14. 是否存在任何固定验证码、模拟短信或假登录成功。

## 七、严禁事项

- 不修改五个一级页面视觉。
- 不重新设计三张账号入口页面。
- 不恢复方形“苒印”。
- 不复制九套创作编辑器。
- 不修改小说数据。
- 不接收或保存第三方平台密码。
- 不上传 `.env.local`、AccessKey、AUTH_SECRET、数据库密码或任何真实密钥。
