# 修改记录 · 2026-09-22 · 创作闭环（排列→组合→连接→节奏→演出）

> 依据：《苒境开发规则.md》。只增量，未删旧逻辑，未改首页/小门动画/白纸拖动/onPointerDown-Up 核心状态机。
> 全程未做"镜自动吸纳"（元素进镜仍由用户明确动作产生，系统只提示不代决）。
> 节奏：系统提供默认节奏（结构生长），用户 perfo-speed-* 可锁定固定值，算法不锁死。

---

## 修改文件（本轮，全部为增量）

| 文件 | 改动 |
|---|---|
| `src/components/creation/PageSheet.tsx` | 替换 2 处 `alert` → 组件内 `flash` 轻提示（1.6s 自隐，抽屉顶部展示） |
| `src/components/creation/Editor.tsx` | 见下"改动明细" |
| `苒境开发规则.md` | 新建：创作形态原则 + 代码判断标准 + 开发硬性规则 |

---

## 改动明细（Editor.tsx）

### 一、基础修复
- `play` 指令里 `alert("请先在排列里选一个分格版式")` → `showCanvasFlash`（画布顶部胶囊轻提示，`pointerEvents:none` 不吞手势，1.6s 自隐）
- 新增 `canvasFlash` state + `showCanvasFlash`；渲染在 stage 最外层，zIndex 20

### 二、组合活化（Unit bbox 实时跟随）
- 新增 `syncUnitBboxes(ids)` + `boundsOfForSync(id)`
  - 只重算"成员在集合中"的 Unit.bbox，取成员当前真实坐标的外接盒
  - 成员真实坐标不变（bbox 是引用，不是写回成员）
  - 结果通过 `onUpdateRef.current({ units: nextUnits })` 持久化
- 三个触发点全部增量插入，不碰手势本身：
  1. 排列（align-*）→ `syncUnitBboxes(sel.map(s=>s.id))`
  2. 分布（distribute-h/v）→ `syncUnitBboxes(bs.map(x=>x.s.id))`
  3. 框选整体拖动（dragBox 分支的 moveBox 尾）→ `syncUnitBboxes(boxOriginalMembers ids)`
- 智能合成（synth-card/label/sticky/zone）建 Unit 时本来就写 bbox，未改

### 三、连接活化（关系建立瞬间的克制反馈）
- 连线模式成功建立 elementLink 后，调用新增的 `showFreshLink(fromId, toId)`
- 渲染层：SVG 里加一块"关系刚建立"覆盖层（freshLink state，1s 后自动清空）
  - 两端各画一个小圆环（opacity 0→1→0.85，0.9s）
  - 中间线：`strokeDasharray = 全长`，`--rj-len` CSS 变量驱动 `stroke-dashoffset 全长→0`（0.85s ease-out）
  - 箭头三角渐显（0.9s）
  - 层最外层 `pointerEvents:none`，不拦截任何手势
- 元素最终位置不变（只是视觉反馈，不是移动）

### 四、节奏活化（从结构生长，可被用户覆盖）
- 播放时按镜计算逐帧时长：`700 + min(镜内Unit数,4)*150 + min(镜内关系数,5)*180`
  - "镜内关系数" = 起点或终点落在该镜内的 elementLinks 条数（centerOf + 坐标包含判定）
- 用户已通过 perfo-speed-* 锁定（`window.__ranjingPerfSpeed` 有值）→ 全帧沿用固定 ms，不覆盖
- `playTimingsRef` 存本次播放序列；setTimeout 链式调度（不再是固定 setInterval）
- play-step 逻辑保留（单步推进）；play-stop 同时 `clearTimeout` + `clearInterval`（兼容旧 timer）

### 五、演出关系展开（关系驱动展示顺序）
- 播放时：`story` 线优先依次点亮，其余线按创建序附后 → `activeLinks` 累积
- SVG 渲染层：播放中非点亮的线降到 opacity 0.25（原值 0.35），点亮的线全显
- 标签（label）在播放中仅当该线已点亮才显示，"关系随线展开"
- 不播放时：一切照旧，activeLinks 空、全显

---

## 闭环验证（代码层面）

1. **排列**：`frame-*` / `perfo-order-*` 产出 comicFrames（order + elementIds）
2. **组合**：`synth-*` 产出 Unit（kind/layout/bbox/memberIds），bbox 现在随成员同步
3. **连接**：连线模式产出 elementLinks（fromType/targetType/relType/label），建立时有克制反馈
4. **关系决定展示顺序**：播放时 story 线排前，activeLinks 驱动逐线点亮；镜序受 perfo-order-links 影响
5. **节奏决定过程**：每镜停留时长按结构计算（Unit数 + 关系数），用户可锁定覆盖
6. **演出让作品活起来**：镜逐张 hot → Unit 渐现 → 关系线依次展开 → 标签随关系出现

---

## 遗留（本轮未做，下次）
- 镜自动吸纳：**不做**（规则明确禁止）
- Unit 整体拖动（选 Unit 外框而非成员）：目前 bbox 会跟随，但还不能用外框直接拖动 Unit（需新增独立手势，增量改动较大，建议单独工单）
- 跨页演出（PageLink flow 驱动跨页跳转播放）：data 已有，播放逻辑只处理同页
- `.next` / `out` 构建产物已生成，未 git add（只 add 源码 5 文件范围内 + 新增 md）
